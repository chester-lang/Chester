@define if_then_else_[a: Type](condition: Boolean, then: => a, else: => a): a {
    @case condition {
        True -> then
        False -> else
    }
}

a = if True then 0 else 1

b = if True then {
    0
} else {
    1
}