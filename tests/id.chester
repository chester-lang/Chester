// write in a canonical way
@define id(x) {
  x
}

// quickly define something and we can calculate

id1(x) = x

// we have types and (implicit args)

@define id[a: Type](x: a) {
  x
}

// we write quickly

id[a: Type](x: a) = x


